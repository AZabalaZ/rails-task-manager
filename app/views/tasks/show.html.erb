<h1><%= @task.title %></h1>
<p><%= @task.details %></p>

<%= link_to "Editar tarea", edit_path %>

<%= link_to "volver a las tasks", tasks_path %>

<%= link_to "Delete",
  task_path(@task),
  data: {turbo_method: :delete, turbo_confirm: "Are you sure?"}
%>
